services:
  ai-assistant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./vector_index:/app/vector_index
    restart: unless-stopped
